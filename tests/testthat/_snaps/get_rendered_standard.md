# get_rendered_standard returns rendered standard with valid inputs

    {
      "type": "character",
      "attributes": {},
      "value": ["    print(\"hello\")", "    if (a) {", "        return(NULL)", "    }", "    else {", "        return(1)", "    }"]
    }

